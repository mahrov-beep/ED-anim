// SafeContainer.qtn
component CharacterSafe {
  EntityRef SelfUnitEntity;

  int Width;
  int Height;
}

// ItemOwnerIsSafe.qtn
component ItemOwnerIsSafe { }

// SafeSignals.qtn
enum SafeChangeKind {
  Added = 0,
  Removed = 1,
  Moved = 2
}

signal OnSafeChanged(EntityRef owner, SafeChangeKind change, EntityRef itemEntity);
signal OnSafeResized(EntityRef owner, int width, int height);